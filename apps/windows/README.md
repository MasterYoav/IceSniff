# IceSniff Windows

This app track is reserved for the future native Windows distribution.

The repository direction is app-per-platform:

- `apps/cli`
- `apps/macos`
- `apps/windows`

When Windows work starts, this folder should own its source, packaging, assets, tests, and backend/runtime pieces directly.
